define("local_lernlandkarte/lernlandkarte", ["exports"], (function(_exports) {
    Object.defineProperty(_exports, "__esModule", {
        value: !0
    }),
        _exports.init = void 0;

    _exports.init = function(params) {
        var llkId = "#llk-" + params.id,
            modalId = "#llkmodal-" + params.id,
            img = document.querySelector(llkId + " img"),
            modal = document.querySelector(modalId),
            btn_show = document.querySelector(llkId + " div:first-of-type svg");

        // Add null check for the close button element
        var closeButton = document.querySelector(modalId + " span");
        if (closeButton) {
            closeButton.addEventListener("click", (function() {
                modal.style.display = "none"
            }));
        }

        var closeButtonModalBG = document.querySelector(modalId);
        if (closeButtonModalBG) {
            console.log("Set closeButtonModalBG.addEventListener");
            closeButtonModalBG.addEventListener("click", (function() {
                console.log("Clicked closeButtonModalBG.addEventListener");
                modal.style.display = "none"
            }));
        }

        // Add null check for the show button
        if (btn_show) {
            btn_show.addEventListener("click", (function() {
                if (modal) {
                    modal.style.display = "flex"
                }
            }));
        }

        // Add null check for the image before applying wheelzoom
        if (img && window.wheelzoom) {
            window.wheelzoom(img, {
                zoom: .1,
                maxZoom: !1,
                initialX: params.x,
                initialY: params.y,
                initialZoom: params.zoom
            });
        }
    }
}));

//# sourceMappingURL=lernlandkarte.min.js.map